.root
  display: flex
  align-items: center
  background: rgba(white, .05)
  font-size: 20px
  padding: 15px
  color: rgba(white, .8)

.searchField
  flex: 1 0 auto
  background: none
  transition: all 150ms ease-in-out
  font-size: inherit
  color: white
  border: none
  outline: none !important
  margin-left: 15px

.logo
  height: 1.3em
  margin-right: .5em
